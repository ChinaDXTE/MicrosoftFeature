<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GeofenceApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Class="GeofenceApp.MainPage"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Frame x:Name="ScenarioFrame"/>
    </Grid>
    <Page.TopAppBar>
        <CommandBar>
            <CommandBar.SecondaryCommands>
                <AppBarButton x:Name="Back" Icon="Back" Click="Back_Click"/>
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.TopAppBar>
    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.SecondaryCommands>
                <AppBarButton x:Name="showGeofence" Label="{Binding geo_ShowGeofenceListButtonText}" Icon="View" Click="showGeofence_Click"/>
                <AppBarButton x:Name="setting" Label="{Binding geo_Setting}" Icon="Setting" Click="setting_Click"/>
            </CommandBar.SecondaryCommands> 
        </CommandBar>
    </Page.BottomAppBar>
</Page>
